import { id, list, now, table } from "../state/store.js"; import { trustedEndpointSignal } from "./endpoints.js";
const policies = () => table("emergency_policies"); const evidence = () => table("emergency_evidence"); const replays = () => table("emergency_replays"); export const EMERGENCY_TTL_MS = 5 * 60 * 1000;
const workflow = ["attempt_communication", "assess_responsiveness_when_available", "alert_household", "contact_configured_emergency_contacts", "contact_emergency_services_when_configured_conditions_are_met"];
const validFraction = (value, min, max) => Number.isFinite(Number(value)) && Number(value) >= min && Number(value) <= max;
export function saveEmergencyPolicy(profileId, policy) { if (!Array.isArray(policy.requiredSignalTypes) || policy.requiredSignalTypes.length < 2 || !policy.requiredSignalTypes.every((item) => typeof item === "string" && item.length <= 80) || !validFraction(policy.minimumConfidence ?? .8, 0, 1) || !Number.isInteger(Number(policy.minimumIndependentSources ?? 2)) || Number(policy.minimumIndependentSources ?? 2) < 2 || Number(policy.minimumIndependentSources ?? 2) > 10) throw Object.assign(new Error("Emergency policy fields are invalid."), { status: 400 }); const existing = policy.id && policies().get(policy.id); if (existing && existing.profileId !== profileId) throw Object.assign(new Error("Emergency policy ownership mismatch."), { status: 403 }); const saved = { id: policy.id || id("emergency_policy"), profileId, enabled: policy.enabled === true, requiredSignalTypes: [...new Set(policy.requiredSignalTypes)], minimumConfidence: Number(policy.minimumConfidence ?? .8), minimumIndependentSources: Number(policy.minimumIndependentSources ?? 2), createdAt: existing?.createdAt || now() }; policies().set(saved.id, saved); return saved; }
export function recordEmergencyEvidence(profileId, input) { if (!input.eventId || typeof input.eventId !== "string" || input.eventId.length > 120 || replays().has(`${profileId}:${input.eventId}`) || !input.type || typeof input.type !== "string" || input.type.length > 80 || !validFraction(input.confidence, 0, 1)) throw Object.assign(new Error("Emergency evidence is malformed or replayed."), { status: 400 }); const trusted = trustedEndpointSignal(profileId, input, ["microphone", "sensor", "wearable", "phone", "vehicle"]); const observedAt = input.observedAt || now(); const timestamp = new Date(observedAt).getTime(); if (!Number.isFinite(timestamp) || Math.abs(Date.now() - timestamp) > EMERGENCY_TTL_MS) throw Object.assign(new Error("Emergency evidence is stale or has an invalid timestamp."), { status: 400 }); const item = { id: id("emergency_evidence"), profileId, eventId: input.eventId, sourceId: trusted.sourceId, endpointKind: trusted.endpointKind, type: input.type, confidence: Number(input.confidence), provenance: trusted.provenance, observedAt, expiresAt: new Date(timestamp + EMERGENCY_TTL_MS).toISOString() }; evidence().set(item.id, item); replays().set(`${profileId}:${input.eventId}`, item.expiresAt); return item; }
export function evaluateEmergency({ signals = [], policy } = {}) { if (!policy?.enabled) return { triggered: false, reason: "No authorized emergency policy is enabled.", actions: [] }; const current = Date.now(); const qualifying = signals.filter((signal) => signal.provenance === "registered_endpoint" && Number(signal.confidence) >= policy.minimumConfidence && policy.requiredSignalTypes.includes(signal.type) && new Date(signal.expiresAt).getTime() > current); const types = new Set(qualifying.map((signal) => signal.type)); const sources = new Set(qualifying.map((signal) => signal.sourceId)); if (types.size < policy.requiredSignalTypes.length || sources.size < policy.minimumIndependentSources) return { triggered: false, reason: "Fresh independent trusted evidence threshold was not met.", actions: [] }; return { triggered: true, reason: "Fresh independent trusted evidence threshold was met.", actions: workflow.map((step) => ({ step, status: "proposal_only_requires_authorized_active_connection" })) }; }
export function evaluateSavedEmergency(profileId, policyId) { const policy = policies().get(policyId); if (!policy || policy.profileId !== profileId) throw Object.assign(new Error("Emergency policy not found."), { status: 404 }); return evaluateEmergency({ policy, signals: list("emergency_evidence", (item) => item.profileId === profileId) }); }
