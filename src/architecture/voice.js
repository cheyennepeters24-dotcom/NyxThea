import { now, table } from "../state/store.js";
export const wakeWords = ["nyxthea", "nyx", "nixie"]; export const voiceStates = ["idle", "listening_simulated", "speaking_simulated", "interrupted_simulated", "quiet"];
const sessions = () => table("voice_sessions");
export function assessWakeContext({ phrase = "", confidence = 0, authorizedNicknames = [], contextConfidence = 0 }) { const normalized = phrase.trim().toLowerCase(); const recognized = wakeWords.includes(normalized) || authorizedNicknames.includes(normalized); const safeToRespond = recognized && Number(confidence) >= .85 && Number(contextConfidence) >= .75; return { recognized, safeToRespond, response: safeToRespond ? "simulated_session_may_start_no_audio_provider" : "remain_quiet", provider: "unavailable", audioActive: false }; }
export function transitionVoice(profileId, event) { const previous = sessions().get(profileId)?.state || "idle"; const next = event === "wake" && previous !== "speaking_simulated" ? "listening_simulated" : event === "speak" ? "speaking_simulated" : event === "interrupt" && previous === "speaking_simulated" ? "interrupted_simulated" : event === "quiet" ? "quiet" : event === "reset" ? "idle" : previous; const session = { profileId, state: next, provider: "unavailable", audioActive: false, simulated: true, updatedAt: now() }; sessions().set(profileId, session); return session; }
export function voiceState(profileId) { return sessions().get(profileId) || { profileId, identity: "Nyxthea female voice identity", state: "idle", provider: "unavailable", audioActive: false, simulated: true, supports: ["wake confidence", "simulated state transitions", "future authorized provider adapters"] }; }
