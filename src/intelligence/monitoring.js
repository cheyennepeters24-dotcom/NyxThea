import { list } from "../state/store.js";
export function selfMonitor(profileId,{aiConnected=false}={}){const endpoints=list("endpoints",x=>x.ownerProfileId===profileId);const integrations=list("integration_connections",x=>x.profileId===profileId);const facts=list("world_facts",x=>x.profileId===profileId);const stale=facts.filter(x=>new Date(x.expiresAt).getTime()<=Date.now());return{mode:aiConnected?"available":"degraded",whatStillWorks:["local profiles","local memory","local records","local reasoning","safety evaluation"],unavailable:[...(!aiConnected?["AI conversation"]:[]),"external provider actions","provider-backed voice"],endpoints:{registered:endpoints.length,connected:0},integrations:{registered:integrations.length,connected:integrations.filter(x=>x.state==="active_connection").length},knowledge:{total:facts.length,stale:stale.length},failures:[],assumptions:["Isolate-local state may reset without notice."],retry:aiConnected?null:"Retry when the AI binding or internet service is available."};}
export function explainFailure({operation,error,retryable=true}){return{whatFailed:String(operation||"operation"),why:String(error||"The operation did not complete."),whatStillWorks:"Local, non-provider features remain available.",cannotDo:"Nyxthea will not claim the action succeeded.",retry:retryable?"You can retry after checking the connection or configuration.":"This action requires a new authorization or configuration."};}
