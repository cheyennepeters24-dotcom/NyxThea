import { understand } from "./understanding.js";
import { verifyResearch } from "./research-verification.js";
export const reasoningStages=["understand","research","compare","verify","explain","prepare","ask_permission","act","verify_result","learn"];
export function runReasoningPipeline({message,route,research=[],authorization={action:false}}){const understanding=understand(message);const verified=verifyResearch(research);const comparison=verified.map(item=>({title:item.title,hasSummary:Boolean(item.extract),source:item.url}));const permitted=authorization.action===true;return{understand:{intent:understanding.intent,route},research:{candidates:research.length},compare:{candidates:comparison},verify:{verifiedSources:verified.length,rejectedSources:research.length-verified.length},explain:{ready:true,basis:verified.length?"verified public sources":"authorized context and conversation"},prepare:{status:"proposal_only"},ask_permission:{required:true,status:permitted?"permission_recorded":"awaiting_permission"},act:{permitted,status:permitted?"authorized_not_executed":"blocked_pending_authorization"},verify_result:{status:"not_executed"},learn:{status:"awaiting_verified_outcome"}};}
export function createReasoningRecord(input){const results=runReasoningPipeline(input);return{...results,stages:reasoningStages.map(stage=>({stage,status:results[stage]?.status||"completed"})),actionPolicy:"Actions only occur through active, authorized integrations. Human control is final."};}
